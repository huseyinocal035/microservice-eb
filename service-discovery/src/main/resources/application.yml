spring:
  application:
    name: eurekaserver
  config:
    import: optional:configserver:http://localhost:8071
  sleuth:
    sampler:
      probability: 0.1
  zipkin:
    baseUrl: http://localhost:9411/